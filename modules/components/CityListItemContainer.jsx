import { View } from "react-native";

const CityListItemContainer = ({ children }) => {
  return <View className="h-20 relative m-2 rounded-md">{children}</View>;
};

export default CityListItemContainer;
